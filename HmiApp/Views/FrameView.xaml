<UserControl x:Class="HmiApp.Views.FrameView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <!-- 常驻右侧窄列：报文表(上) + 选中帧逐字段解码(下)，中间可拖动分隔 -->
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*" MinHeight="120"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*" MinHeight="120"/>
        </Grid.RowDefinitions>

        <!-- 工具条 -->
        <Border Grid.Row="0" Background="{StaticResource Panel}" BorderBrush="{StaticResource Border}" BorderThickness="1" CornerRadius="4" Padding="6,4" Margin="0,0,0,4">
            <StackPanel>
                <TextBlock Text="收发报文 (Modbus)" Style="{StaticResource H1}"/>
                <WrapPanel Margin="0,4,0,0">
                    <CheckBox Content="记录轮询读" IsChecked="{Binding LogPolling}"/>
                    <ToggleButton Content="暂停" IsChecked="{Binding FramePaused}" Width="48" Margin="8,0,0,0"/>
                    <Button Content="清空" Command="{Binding ClearFramesCommand}" Width="48" Margin="6,0,0,0"/>
                </WrapPanel>
            </StackPanel>
        </Border>

        <!-- 报文列表 -->
        <DataGrid Grid.Row="1" ItemsSource="{Binding Frames}" SelectedItem="{Binding SelectedFrame}"
                  FontFamily="Consolas" FontSize="12" GridLinesVisibility="Horizontal">
            <DataGrid.RowStyle>
                <Style TargetType="DataGridRow">
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding IsWrite}" Value="True">
                            <Setter Property="Background" Value="#3A2F12"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </DataGrid.RowStyle>
            <DataGrid.Columns>
                <DataGridTextColumn Header="时间" Binding="{Binding TimeText}" Width="86"/>
                <DataGridTextColumn Header="向" Binding="{Binding DirText}" Width="40"/>
                <DataGridTextColumn Header="摘要" Binding="{Binding Summary}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>

        <!-- 可拖动横向分隔 -->
        <GridSplitter Grid.Row="2" Height="5" HorizontalAlignment="Stretch" VerticalAlignment="Center"
                      Background="#1A3358" ResizeBehavior="PreviousAndNext" ResizeDirection="Rows"/>

        <!-- 选中帧逐字段解码 -->
        <DockPanel Grid.Row="3">
            <TextBlock DockPanel.Dock="Top" Text="选中帧 · 逐字段解码 (MBAP + PDU)" Style="{StaticResource H1}" Margin="0,2,0,4"/>
            <TextBox Text="{Binding SelectedFrameDetail, Mode=OneWay}" IsReadOnly="True"
                     FontFamily="Consolas" FontSize="12" TextWrapping="NoWrap"
                     VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto"
                     Background="#0A1626" Foreground="#CFE3F5" Padding="6"/>
        </DockPanel>
    </Grid>
</UserControl>
