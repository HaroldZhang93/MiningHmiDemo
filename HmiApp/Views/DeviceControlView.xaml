<UserControl x:Class="HmiApp.Views.DeviceControlView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <DockPanel Margin="4">
        <!-- 顶部提示 -->
        <Border DockPanel.Dock="Top" Style="{StaticResource Card}" Margin="6,6,6,2">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="设备控制台" Style="{StaticResource H1}"/>
                <TextBlock Text="（控制经 SlaveSim 连接下发，请先在顶部「连接」）" Foreground="{StaticResource SubText}" Margin="10,0,0,0"/>
                <TextBlock Text="{Binding CtrlHint}" Foreground="{StaticResource Warn}" Margin="16,0,0,0"/>
            </StackPanel>
        </Border>

        <ScrollViewer VerticalScrollBarVisibility="Auto">
            <ItemsControl ItemsSource="{Binding Controls}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate><WrapPanel/></ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Style="{StaticResource Card}" Width="300">
                            <StackPanel>
                                <!-- 标题 + 运行灯 -->
                                <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                                    <Ellipse Width="14" Height="14" Margin="0,0,8,0"
                                             Visibility="{Binding ShowLamp, Converter={StaticResource BoolToVis}}">
                                        <Ellipse.Style>
                                            <Style TargetType="Ellipse">
                                                <Setter Property="Fill" Value="#555"/>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding RunPoint.IsOn}" Value="True">
                                                        <Setter Property="Fill" Value="{StaticResource Ok}"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Ellipse.Style>
                                    </Ellipse>
                                    <TextBlock Text="{Binding Device}" Style="{StaticResource H1}"/>
                                </StackPanel>

                                <!-- 关键读数 -->
                                <ItemsControl ItemsSource="{Binding Readings}" Margin="0,0,0,6">
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <DockPanel Margin="0,1">
                                                <TextBlock Text="{Binding Name}" Foreground="{StaticResource SubText}" Width="120"/>
                                                <TextBlock>
                                                    <Run Text="{Binding Value, Mode=OneWay}" Foreground="#E6ECF5"/>
                                                    <Run Text="{Binding Unit, Mode=OneWay}" Foreground="#8FA6C4"/>
                                                </TextBlock>
                                            </DockPanel>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>

                                <!-- 启停开关 -->
                                <StackPanel Orientation="Horizontal" Margin="0,2"
                                            Visibility="{Binding HasStartStop, Converter={StaticResource BoolToVis}}">
                                    <Button Content="{Binding StartLabel}" Command="{Binding StartCommand}" Width="120" Background="{StaticResource Ok}"/>
                                    <Button Content="{Binding StopLabel}"  Command="{Binding StopCommand}"  Width="120" Background="{StaticResource Alarm}"/>
                                </StackPanel>

                                <!-- 设定值：滑块 + 输入框 + 下发 -->
                                <StackPanel Margin="0,4" Visibility="{Binding HasSetpoint, Converter={StaticResource BoolToVis}}">
                                    <TextBlock Text="{Binding SetpointLabel}" Foreground="{StaticResource SubText}" Margin="0,0,0,2"/>
                                    <DockPanel>
                                        <TextBox DockPanel.Dock="Right" Width="56" Margin="6,0,0,0"
                                                 Text="{Binding Setpoint, StringFormat=F1, UpdateSourceTrigger=PropertyChanged}"/>
                                        <Button DockPanel.Dock="Right" Content="下发" Command="{Binding SendCommand}" Width="56" Margin="6,0,0,0"/>
                                        <Slider Minimum="{Binding SpMin}" Maximum="{Binding SpMax}" Value="{Binding Setpoint}"
                                                VerticalAlignment="Center" IsSnapToTickEnabled="False"/>
                                    </DockPanel>
                                </StackPanel>

                                <!-- 动作按钮（升柱/降柱/移架 等）-->
                                <ItemsControl ItemsSource="{Binding Actions}" Margin="0,2"
                                              Visibility="{Binding HasActions, Converter={StaticResource BoolToVis}}">
                                    <ItemsControl.ItemsPanel>
                                        <ItemsPanelTemplate><WrapPanel/></ItemsPanelTemplate>
                                    </ItemsControl.ItemsPanel>
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <Button Content="{Binding Label}" Command="{Binding Command}" Width="78" Margin="0,0,6,6"/>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </DockPanel>
</UserControl>
